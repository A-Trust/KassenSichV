Please copy the

 - configuration file (asigntseonline.conf) from the <install-dir>\config directory
 - the native library (asigntse.dll) and the object file library (asigntse.dll.lib) from the respective (*) <install-dir>\bin directory

to the extracted directory c/Example/Example.

(*) When building x86 bit version, please use the dll from a.sign TSE\bin\win32-cdecl\